# FIRST_START.md

Read this only when `tasks.json` is missing.

Create `GOAL.md`, `tasks.json`, `progress.txt`, `SUBTASKS.md`, and the `spec/` files listed in `GOAL.md`. Initialize every task with dependencies, acceptance criteria, verification, notes, and timestamps. Mark only the initialization task complete after parsing `tasks.json` and confirming every mapped file exists. Append the initialization evidence to `progress.txt`, then begin the next actionable task because this build run explicitly requests loop completion.
