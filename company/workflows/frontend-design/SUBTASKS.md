# SUBTASKS.md

Decompose only when a task cannot be completed and verified in one focused iteration.

Each subtask must have an ID, name, description, status, dependencies, order, `spec_ref`, acceptance criteria, verification method, notes, and timestamps. A parent remains `in_progress` until every subtask and the parent-level verification pass. Add discovered work to `tasks.json`; do not hide it in prose.
