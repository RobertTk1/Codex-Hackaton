{
  "name": "magic-mirror",
  "version": "0.0.0",
  "private": true,
  "packageManager": "bun@1.3.11",
  "engines": {
    "bun": ">=1.3.11"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "preinstall": "bun scripts/validate-workspaces.ts",
    "validate:workspaces": "bun scripts/validate-workspaces.ts",
    "dev:web": "bun run --cwd apps/web dev",
    "build:web": "bun run --cwd apps/web build",
    "typecheck:web": "bun run --cwd apps/web typecheck",
    "dev:api": "bun run --cwd apps/api dev",
    "start:api": "bun run --cwd apps/api start",
    "build:api": "bun run --cwd apps/api build",
    "typecheck:api": "bun run --cwd apps/api typecheck",
    "typecheck": "tsc --noEmit --project tsconfig.json && bun run typecheck:web && bun run typecheck:api",
    "test": "bun test",
    "check": "bun run validate:workspaces && bun run typecheck && bun test"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
